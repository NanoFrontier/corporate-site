{
  "name": "magenta-meteorite",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "astro build && git worktree add dist origin/gh-pages && cp -r dist/* dist/.git/* dist"
  },
  "dependencies": {
    "astro": "^5.7.13"
  },
  "devDependencies": {
    "@astrojs/tailwind": "^6.0.2",
    "tailwindcss": "^3.4.17"
  }
}
